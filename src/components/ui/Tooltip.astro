---
export interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span class="astro-tooltip astro-ui">
  <slot />
  <span class="astro-tooltip__bubble" role="tooltip">{label}</span>
</span>
